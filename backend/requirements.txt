# Web framework
fastapi>=0.115
uvicorn[standard]>=0.30
websockets>=12.0

# Config & validation
pydantic>=2.7
pydantic-settings>=2.3

# Data / market
alpaca-py>=0.30
yfinance>=0.2.40
pandas>=2.2
numpy>=1.26

# Sentiment
vaderSentiment>=3.3.2

# Optional LLM sentiment backend (off by default). Drives Claude via the local
# Claude Code CLI subscription — requires the `claude` CLI installed & logged in.
claude-agent-sdk>=0.1

# Persistence
SQLAlchemy>=2.0

# Scheduling
APScheduler>=3.10

# HTTP
httpx>=0.27

# Testing
pytest>=8.0
